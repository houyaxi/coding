# 学习路线（详细版）

本路线从零基础出发，逐步覆盖 Python 编程、数据处理、深度学习模型构建与大模型应用。每一阶段都配有讲义、示例代码、练习任务和参考答案。

## 阶段 1：Python 基础

目标：写出并运行第一段程序，掌握最常用的基础语法。

内容：
- print 输出：多参数、sep、end、f-string 格式化
- 变量与赋值：命名规则、动态类型
- import 导入：import 模块、from 模块 import 名字、as 别名
- 常用标准库：math、random、datetime、os
- 基础数据类型：int、float、str、bool、list、dict、tuple、set
- 类型判断与转换：type()、int()、str()

练习：见 stage_01_python基础/practice_01.py

库速查卡：无第三方库，记住内置函数即可。

## 阶段 2：控制流与函数

目标：用代码表达逻辑判断与重复操作。

内容：
- if / elif / else 条件判断
- for 循环、range()、while 循环、break / continue
- 函数定义 def、参数、返回值、默认参数
- lambda 匿名函数
- 列表推导式

## 阶段 3：class 面向对象

目标：理解面向对象思想，能定义和使用类。

内容：
- 类的定义：class、__init__、self
- 实例属性与方法
- 类属性、静态方法、类方法
- 继承与重写
- 常用魔法方法：__str__、__repr__、__len__、__eq__

## 阶段 4：NumPy 数据处理

目标：掌握数组这种核心数据结构，能进行批量数值运算。

内容：
- 创建数组：np.array、np.zeros、np.ones、np.arange、np.linspace、np.random
- 形状与变形：shape、reshape、转置
- 索引与切片、布尔掩码
- 数学运算：加减乘除、sum、mean、max、min、dot
- 广播机制
- 保存与读取：np.save、np.load

库速查卡：创建、形状、索引、统计、矩阵运算各记 3-5 个常用函数。

## 阶段 5：Pandas 数据清洗

目标：能处理真实表格数据。

内容：
- Series 与 DataFrame
- 读取与写入：read_csv、read_excel、to_csv
- 查看数据：head、info、describe
- 筛选与排序：loc、iloc、sort_values
- 缺失值处理：dropna、fillna
- 分组聚合：groupby、agg
- 合并：merge、concat

## 阶段 6：结果展示（matplotlib）

目标：把数据和模型结果画成图。

内容：
- 折线图、柱状图、散点图、直方图
- 标题、坐标轴、图例、网格
- 子图布局 subplot
- 保存图片 savefig
- 中文显示设置

## 阶段 7：PyTorch 入门

目标：理解深度学习框架的基础构件。

内容：
- 张量创建与运算：torch.tensor、torch.zeros、torch.randn
- 张量与 NumPy 互转
- 形状操作：view、reshape、permute
- 自动求导：requires_grad、backward、grad
- 设备：CPU / GPU（cuda）
- 数据加载：Dataset、DataLoader

## 阶段 8：深度学习模型构建

目标：亲手构建并训练一个神经网络。

内容：
- nn.Module 与 nn.Linear、nn.ReLU、nn.Sequential
- 损失函数：MSE、CrossEntropy
- 优化器：SGD、Adam
- 训练循环：forward、loss、backward、optimizer.step
- 线性回归、多层感知机 MLP
- 卷积网络 CNN 入门
- 模型保存与加载

## 阶段 9：Transformer 原理与实现

目标：理解现代大模型的核心架构。

内容：
- 注意力机制：Q、K、V、缩放点积注意力
- 多头注意力
- 位置编码
- 前馈网络与残差连接、LayerNorm
- 从零实现一个可训练的小型 Transformer
- 用小型 Transformer 做一个文本任务（如字符级生成）

## 阶段 10：大模型应用（transformers）

目标：会用 HuggingFace 生态加载和使用预训练模型。

内容：
- AutoTokenizer 与 AutoModel
- 文本分类、文本生成 pipeline
- prompt 编写与 few-shot
- 模型下载与缓存管理
- 简单微调（可选）

## 阶段 11：综合小项目

目标：独立完成一个完整项目，串起所有知识。

候选项目（按难度递增）：
- 数值统计与可视化报告
- 表格数据清洗 + 特征分析 + 图表展示
- 用 PyTorch 训练一个手写数字识别模型并展示结果
- 用小型 Transformer 训练一个文本生成模型
- 加载一个开源大模型完成指定任务（如摘要、翻译、问答）

## 学习节奏建议

- 阶段 1-3：每阶段 1-2 天，重点是多动手。
- 阶段 4-6：每阶段 2-3 天，练习真实数据处理。
- 阶段 7-8：每阶段 3-5 天，深度学习概念需要反复理解。
- 阶段 9-10：每阶段 1 周左右，Transformer 与大模型内容较多。
- 阶段 11：不限时，作为毕业项目。

每学完一个库，翻回本文件的库速查卡索引，自己默写常用函数，写不出的再查，反复几次就能记住。
